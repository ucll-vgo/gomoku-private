<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        xmlns:vm="clr-namespace:ViewModel.Screens;assembly=ViewModel"
        xmlns:screen="clr-namespace:View.Screens"
        mc:Ignorable="d"
        Title="Gomoku" Height="800" Width="800"
        Background="{DynamicResource Background}"
        x:Name="main">
    <ContentControl Content="{Binding Value}">
        <ContentControl.Resources>
            <DataTemplate DataType="{x:Type vm:IntroductionScreenViewModel}">
                <screen:IntroductionScreen />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsScreenViewModel}">
                <screen:SettingsScreen />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:PlayScreenViewModel}">
                <screen:PlayScreen />
            </DataTemplate>
        </ContentControl.Resources>
    </ContentControl>
</Window>
